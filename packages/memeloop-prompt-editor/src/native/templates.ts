/**
 * React Native Paper-based templates for RJSF.
 * Minimal templates; extend or override as needed.
 */

import type { TemplatesType } from "@rjsf/utils";

/** Base templates: use RJSF default behavior; override with Paper components in app */
export const templates: Partial<TemplatesType> = {
  // Consumers can merge in FieldTemplate, ObjectFieldTemplate, etc. using Paper components
};
